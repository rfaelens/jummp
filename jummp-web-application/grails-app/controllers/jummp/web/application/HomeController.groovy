package jummp.web.application

/**
 * Controller other Controllers can redirect to if the request is not an AJAX request.
 * This Controller takes care of generating the required JavaScript to load the correct
 * state of the Web Application.
 */
class HomeController {

    def index = { }
}
